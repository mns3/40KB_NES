# 40KB_NES

> 40KB ROM 제약 안에서 동작하는 NES 2인 대전 플랫포머

![메인 타이틀 화면](nes.png)

| 항목 | 내용 |
| :--- | :--- |
| 플랫폼 | NES / Famicom (NROM-256 매퍼) |
| 언어 | C (cc65), 6502 Assembly |
| 최종 ROM | **40,976 bytes** |
| 개발 기간 | 2024.11 ~ 2025.01 (10주) |
| 팀 규모 | 2인 |

<br>

## 🎮 프로젝트 소개

440KB_NES는 과거 패미컴 게임의 용량이 40KB만으로 게임을 제작해보고자 시작한 프로젝트로 두 플레이어가 한 화면에서 경쟁하는 8비트 플랫포머 게임입니다. 저희는 용량 뿐만 아니라 실제 NES(패미컴)에서 작동하는 게임을 제작하고자 하여 라즈베리파이를 이용한 NES 환경과 컨트롤러를 직접 제작하였습니다.

<br>

## 📌 이 저장소의 출발점과 작업 범위

이 프로젝트는 **공개된 예제 코드를 분석해 확장한 결과물** 입니다. 기반이 된 코드는 Doug Fraker(nesdoug)의 cc65용 메타타일 예제이며, 원저작자 표기는 [`metatiles.c`](metatiles.c) 최상단에 그대로 유지하고 있습니다.
또한 그래픽 에셋은 Morphcat Games의 **Micro Mages** 에셋을 가공하여 사용하였습니다

```c
/*  example code for cc65, for NES
 *  draw a BG with metatile system
 *  , also sprite collisions with BG
 *  using neslib
 *  Doug Fraker 2018
 */
```

원본은 1인용 이동·충돌 데모였고, 게임으로서의 규칙과 승패 판정, 2인 대전 구조는 없었습니다.
### 원본에서 확장한 부분

| 구분 | 내용 |
| :--- | :--- |
| 2인 대전 구조 | 컨트롤러 2개 입력을 각각 처리하고, 플레이어별 물리·충돌 루프와 상태 변수를 분리 |
| 게임 규칙 | 타일 속성으로 함정·목표를 판정해 사망 리스폰과 승패 결정 로직 구현 |
| 다목적 버튼 | 상태에 따라 같은 액션이 달라지는 다목적 버튼 추가 |
| 애니메이션 | 방향·정지·모션 3축 상태 분기와 프레임 카운터 기반 스프라이트 순환 |
| 버그 수정 | 고정소수점 좌표의 부호 없는 정수 오버플로로 발생한 화면 순간이동 문제 해결 |
| 빌드 자동화 | Tiled 맵의 CSV 익스포트를 C 배열 소스로 변환하는 Python 스크립트 작성 |
| 실행 환경 | Raspberry Pi 4 + RetroPie 구성, 케이스 3D 프린팅, GPIO 물리 컨트롤러 제작 |

<br>

## 🛠 Tech Stack

| Category | Technology | Description |
| :--- | :--- | :--- |
| Language | C (cc65), 6502 Assembly | 게임 로직은 C, 하드웨어 근접 부분은 어셈블리 |
| Toolchain | cc65 / ca65 / ld65 | 6502 크로스 컴파일러 · 어셈블러 · 링커 |
| Level Design | Tiled + 자체 Python 스크립트 | 맵 편집 후 C 배열로 자동 변환 |
| Runtime | Raspberry Pi 4 + RetroPie | 전용 실행 환경 및 GPIO 컨트롤러 |

<br>

## ⚙️ 기술 구현 상세

### 1. 8.8 고정소수점 좌표계

6502에는 부동소수점 연산 유닛이 없습니다. 그런데도 가속과 관성이 있는 이동감을 만들기 위해, 16비트를 상위 8비트(정수부)와 하위 8비트(소수부)로 나눠 쓰는 **8.8 고정소수점** 을 좌표계로 사용했습니다.

```c
#define SPEED 0x180          // 1.5 px/frame  (0x180 >> 8 = 1, 나머지 0x80 = 0.5)

signed int hero_velocity_x;  // signed, low byte is sub-pixel
signed int hero_velocity_y;
```

- 상위 바이트만 읽으면 그대로 픽셀 좌표가 되므로, 렌더링 시 변환 비용이 없습니다.
- 가감속과 중력, 낙하 속도 상한을 소수부 누산으로 구현했습니다.

### 2. 타일 기반 AABB 충돌과 Ejection 보정

캐릭터의 코너 좌표에 해당하는 타일 속성만 검사하는 AABB 방식으로 충돌 판정 연산량을 억제했습니다.

충돌이 감지되면 **비트 연산으로 유효 좌표를 즉시 계산해 밀어내는 Ejection 처리** 로 지형에 끼이는 현상을 방지합니다. 타일 크기가 2의 거듭제곱이라 나눗셈 없이 마스킹으로 처리됩니다.

```c
eject_L = temp_x | 0xf0;         // 좌측 충돌 시 타일 경계로 스냅
eject_R = (temp_x + 1) & 0x0f;   // 우측 충돌 시 잔여 오프셋만 추출

high_byte(BoxGuy1.x) = high_byte(BoxGuy1.x) - eject_L;
```

### 3. 버튼 2개 제약을 상태 기반 분기로 해결

NES 컨트롤러의 액션 버튼은 A와 B 두 개뿐입니다. 버튼을 늘릴 수 없으니 **상태에 따라 다르게 액션하는 다목적 버튼을 제작** 했습니다.

방향키를 누르지 않은 상태의 지상에서는 멀리뛰기 점프가, 방향키를 누른 상태의 지상에서는 작게 뛰는 점프가, 공중에서는 2단 점프가 발동하여 2개의 버튼만으로 다양한 액션을 구사할 수 있습니다.

### 4. 레벨 데이터 파이프라인 자동화

맵을 수정할 때마다 타일 인덱스를 손으로 옮기는 작업이 반복됐습니다. [`BG/CSV2C.py`](BG/CSV2C.py) 를 작성해 **Tiled의 CSV 익스포트를 C 배열 소스로 자동 변환** 하도록 만들어, 맵 편집과 빌드 사이의 수작업을 제거했습니다.

<br>

## 🎛 실행 환경

에뮬레이터 외에 전용 실행 환경을 직접 구성했습니다.

| 구성 | 내용 |
| :--- | :--- |
| 본체 | Raspberry Pi 4 + RetroPie |
| 케이스 | 직접 3D 모델링해 프린팅한 전용 케이스 |
| 입력 | 브레드보드에 택트 스위치를 배선한 컨트롤러 |
| 연결 | GPIO 핀 입력을 게임 컨트롤러 입력으로 매핑 |

<p align="center">
  <img src="console.png" width="45%" alt="Raspberry Pi 4 기반 본체">
  <img src="controller.png" width="45%" alt="GPIO 브레드보드 컨트롤러">
</p>


<br>

## 📁 저장소 구조

```text
.
├── metatiles.c              # 게임 로직 (원본 예제 기반 확장)
├── metatiles.h              # 전역 변수 · 제로페이지 세그먼트 정의
├── Sprites.h                # 메타스프라이트 정의
├── metatiles.chr            # CHR ROM 타일 데이터 (8KB)
├── crt0.s                   # 스타트업 코드
├── nrom_32k_vert.cfg        # 링커 설정 (PRG 32KB + CHR 8KB)
├── makefile / compile.bat
├── BG/
│   ├── untitled.tmx         # Tiled 맵 원본
│   ├── CSV2C.py             # CSV → C 배열 변환 스크립트
│   └── Room1.c              # 변환 결과
├── NES_ST/
│   ├── meta.py              # 메타타일 데이터 처리 스크립트
│   └── metatiles.bmp/.nam/.txt
├── LIB/
│   ├── neslib.h/.s          # NES 하드웨어 추상화 (Shiru)
│   └── nesdoug.h/.s         # 보조 라이브러리 (Doug Fraker)
└── BUILD/
    ├── last.nes             # 최종 ROM (40,976 bytes)
    ├── metatiles.s          # 컴파일러가 생성한 어셈블리
    └── labels.txt           # 링커 심볼 맵
```

<br>

## 📝 개발 과정

| 회차 | 단계 | 내용 |
| :---: | :--- | :--- |
| 1 | 회의 | 프로젝트 구체화, 개발 환경 · 언어 · 일정 결정 |
| 2 | 설계 | 게임 구조 설계 |
| 3 | 타일맵 · 스프라이트 | 그래픽 데이터 파이프라인 구축 |
| 4 | 애니메이션 | 스프라이트 애니메이션 구현 |
| 5 | 물리 | 이동 · 중력 · 충돌 구현 |
| 6 | 게임 디자인 | 규칙 및 승패 조건 설계 |
| 7 | 레벨 디자인 | 스테이지 구성 |
| 8 | 완성 보고서 | 결과 정리 및 회고 |

<br>


## 👥 팀 구성

| 이름 | 포지션 | 담당 업무 |
| :---: | :--- | :--- |
| **박민성** | 프로젝트 리드 | 게임 로직, 물리 · 충돌, 애니메이션, 빌드 자동화, 실행 환경 구성 |
| **배도영** | 프로그래머 / 그래픽 | 그래픽 데이터 작업, 개발 보조 |

<br>

## ⚠️ 라이선스 및 저작권

기술 학습 및 포트폴리오 목적의 **비상업적(Non-commercial)** 프로젝트입니다. 수익을 창출하거나 판매하지 않습니다.

**본인 저작 범위**

저장소의 [`LICENSE`](LICENSE)(MIT)는 이 프로젝트에서 직접 작성한 코드에만 적용됩니다. 아래 제3자 코드와 에셋에는 적용되지 않으며, 각 원저작자의 권리가 유지됩니다.

**기반 코드**

| 대상 | 원저작자 |
| :--- | :--- |
| 메타타일 예제 코드 (`metatiles.c` 기반) | Doug Fraker (nesdoug) |
| `LIB/neslib.h`, `LIB/neslib.s` | Shiru |
| `LIB/nesdoug.h`, `LIB/nesdoug.s` | Doug Fraker |

**그래픽 에셋**

그래픽 리소스는 Morphcat Games의 **Micro Mages** 에셋을 가공하여 활용하였습니다. 해당 에셋의 저작권은 원작자에게 있으며, 재배포나 상업적 이용을 허락하지 않습니다. 본 프로젝트는 뛰어난 8-bit 게임인 원작의 그래픽 스타일과 최적화 방식을 학습하고 존경을 표하는 **오마주(Homage)** 성격을 가집니다.
